# .PHONY: all install clean

# .SUBDIRS:

MAIN = test
LIB  = $(LIBARES)
MAINSRC = $(MAIN)

CXXFILES[] = $(filter-out $(MAINSRC), $(removesuffix $(ls *.cpp)))

.DEFAULT: $(CXXProgram $(MAIN), $(CXXFILES) $(MAINSRC)) \
$(StaticCXXLibrary $(LIB), $(CXXFILES))
